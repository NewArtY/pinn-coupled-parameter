# Exact versions of the environment used to regenerate the figures of the
# revised manuscript (Windows 11, Python 3.13.3).
#
# The archived checkpoints in checkpoints/ were trained on
# Windows 11 / Python 3.13.3 / PyTorch 2.6.0 + CUDA 12.4 (NVIDIA GeForce
# RTX 4060 Laptop GPU); the analytical figures (Figs. 1-9, 13) do not depend
# on PyTorch and reproduce identically on CPU.  PyTorch is pinned to the
# training build (2.6.0) so that the Fig. 10-12 checkpoints load bit-for-bit;
# install the matching CPU or CUDA wheel for your platform.
numpy==2.4.4
scipy==1.17.1
matplotlib==3.10.9
scikit-learn==1.8.0
torch==2.6.0
pytest==8.3.4
