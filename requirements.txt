# Minimum versions known to work.  For an exact reproduction of the archived
# checkpoints and figures use requirements-lock.txt instead.
torch>=2.2
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7
numpy>=1.24
pytest>=7.0
