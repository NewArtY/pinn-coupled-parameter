name: pinn-coupled-parameter
channels:
  - conda-forge
dependencies:
  - python=3.13
  - numpy>=1.24
  - scipy>=1.10
  - matplotlib>=3.7
  - scikit-learn>=1.3
  - pytest>=7.0
  - pip
  - pip:
      # CPU build; for CUDA use
      #   pip install torch --index-url https://download.pytorch.org/whl/cu124
      - torch>=2.2
